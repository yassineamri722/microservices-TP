apiVersion: v1
kind: Service
metadata:
  name: my-country-service
  labels:
    app: my-country-service
spec:
  type: NodePort
  selector:
    app: my-country-service
  ports:
    - port: 8082
      targetPort: 8080
      nodePort: 31932
